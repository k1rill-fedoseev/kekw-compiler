# TEST QUOTE
(setq x (plus 1 2)) # x gets the value of 3
x # the value of 3
(quote x) # the atom x itself
(setq y (quote (plus 1 2))) # y gets the list (plus 1 2)
y
(quote 5) # the same as 5
6 # the value of 6